import { Outlet } from 'react-router-dom';

import { FC } from 'react';


const SignUpPagesLayout: FC = () => {
  return (
    < >
      <Outlet />
    </>

  );
};

export default SignUpPagesLayout;
